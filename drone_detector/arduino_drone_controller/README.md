# Arduino Drone Controller

This folder is reserved for the Arduino-based drone control code that will replace the archived turret firmware.

The old turret firmware is preserved at:

```text
archived/arduino_turret_firmware_legacy.zip
```

Add the control mapping here before writing firmware:

```text
Input / command:
Arduino pin:
Drone control:
Expected signal:
Safety notes:
```
